<%= f.collection_check_boxes :user_ids, @team.users, :id, :name do |b| %>
  <tr>
    <%= b.label do %>
      <td><%= b.text %></td>
      <td class="selection_checkbox"><%= b.check_box %></td>
    <% end %>
  </tr>
<% end %>
