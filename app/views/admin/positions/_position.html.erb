<tr>
  <td><%= position.id %></td>
  <td><%= position.name %></td>
  <td><%= position.abbreviation %></td>
  <td>
    <%= link_to "", edit_admin_position_path(position), class: "glyphicon glyphicon-edit" %>
    <%= link_to "", admin_position_path(position.id), method: :delete, data: {confirm: t("yas")}, 
                    class: "glyphicon glyphicon-trash" %>
  </td>
</tr>
